/**
 * Phoenix travel-agency flight map (Mapbox) — disabled in this Electron app.
 * Keeps default export so theme build imports do not break.
 */
const flightMapInit = () => {};

export default flightMapInit;
